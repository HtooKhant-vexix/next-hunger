import AddMenu from "@/components/AddMenu";
import Layout from "@/components/BackofficeLayout";
import React from "react";

const MenuPage = () => {
  return (
    <Layout>
      <AddMenu label="Add Menu" />
    </Layout>
  );
};

export default MenuPage;
